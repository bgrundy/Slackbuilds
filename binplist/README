binplist (python module)

A binary property list (plist) parser module written in python.

This will build the Python 3 modules.  If you want to also build modules for
Python 2, use:

  # PYTHON2=yes ./binplist.SlackBuild
